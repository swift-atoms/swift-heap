description: "Heap — a binary heap / priority queue with configurable min/max ordering (O(log n) push/pop, O(1) peek) over move-only or copyable elements."
topics:
  - molecules
  - heap
homepage: "https://swift-institute.org"
readme:
  family: E
